apiVersion: flow.gideas.io/v1
kind: FoundryFlow
metadata:
  labels:
    app.kubernetes.io/name: operator
    app.kubernetes.io/managed-by: kustomize
  name: foundryflow-sample
spec:
  # TODO(user): Add fields here
