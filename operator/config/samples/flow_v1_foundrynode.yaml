apiVersion: flow.gideas.io/v1
kind: FoundryNode
metadata:
  labels:
    app.kubernetes.io/name: operator
    app.kubernetes.io/managed-by: kustomize
  name: foundrynode-sample
spec:
  # TODO(user): Add fields here
