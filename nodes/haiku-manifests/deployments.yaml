# Haiku Foundry Cycle — Node Deployments
#
# Each node runs as a Deployment with two containers:
#   1. The node container (user code) — runs on :50053
#   2. The sidecar container — runs on :50051, proxies to services
#
# The sidecar connects to:
#   - Operator at OPERATOR_ADDRESS (default: flow-operator:50052)
#   - Archivist at ARCHIVIST_ADDRESS (default: flow-archivist:50054)
#   - Librarian at LIBRARIAN_ADDRESS (default: flow-librarian:50058)
#   - Event Bus at EVENT_BUS_ADDRESS (default: flow-eventbus:50056)
#   - Friction Ledger at FRICTION_LEDGER_ADDRESS (default: flow-frictionledger:50057)
#
# Prerequisites:
#   - Build node images (one per node)
#   - Build sidecar image: docker build -t flow-sidecar:latest -f sidecar/Dockerfile .
#   - Deploy system services (operator, archivist, librarian, eventbus, frictionledger, monitor)
#   - Apply flow.yaml first (CRDs must exist before deployments)
---
# --- Forge ---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: forge
  namespace: default
  labels:
    app.kubernetes.io/name: forge
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: forge
  template:
    metadata:
      labels:
        app.kubernetes.io/name: forge
        flow.gideas.io/node-name: forge
    spec:
      containers:
        - name: node
          image: forge:latest
          ports:
            - containerPort: 50053
          env:
            - name: FLOW_NODE_PORT
              value: "50053"
            - name: OLLAMA_BASE_URL
              value: "http://ollama:11434"
          volumeMounts:
            - name: node-config
              mountPath: /etc/foundry
              readOnly: true
        - name: sidecar
          image: flow-sidecar:latest
          ports:
            - containerPort: 50051
          env:
            - name: FLOW_NODE_ID
              value: "forge"
            - name: FLOW_SIDECAR_PORT
              value: "50051"
            - name: OPERATOR_ADDRESS
              value: "flow-operator:50052"
            - name: ARCHIVIST_ADDRESS
              value: "flow-archivist:50054"
            - name: LIBRARIAN_ADDRESS
              value: "flow-librarian:50058"
            - name: EVENT_BUS_ADDRESS
              value: "flow-eventbus:50056"
            - name: FRICTION_LEDGER_ADDRESS
              value: "flow-frictionledger:50057"
      volumes:
        - name: node-config
          configMap:
            name: forge-config
---
# --- Quench ---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: haiku-quench
  namespace: default
  labels:
    app.kubernetes.io/name: haiku-quench
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: haiku-quench
  template:
    metadata:
      labels:
        app.kubernetes.io/name: haiku-quench
        flow.gideas.io/node-name: quench
    spec:
      containers:
        - name: node
          image: haiku-quench:latest
          ports:
            - containerPort: 50053
          env:
            - name: FLOW_NODE_PORT
              value: "50053"
        - name: sidecar
          image: flow-sidecar:latest
          ports:
            - containerPort: 50051
          env:
            - name: FLOW_NODE_ID
              value: "quench"
            - name: FLOW_SIDECAR_PORT
              value: "50051"
            - name: OPERATOR_ADDRESS
              value: "flow-operator:50052"
            - name: ARCHIVIST_ADDRESS
              value: "flow-archivist:50054"
---
# --- Sort ---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: sort
  namespace: default
  labels:
    app.kubernetes.io/name: sort
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: sort
  template:
    metadata:
      labels:
        app.kubernetes.io/name: sort
        flow.gideas.io/node-name: sort
    spec:
      containers:
        - name: node
          image: sort:latest
          ports:
            - containerPort: 50053
          env:
            - name: FLOW_NODE_PORT
              value: "50053"
          volumeMounts:
            - name: node-config
              mountPath: /etc/foundry
              readOnly: true
        - name: sidecar
          image: flow-sidecar:latest
          ports:
            - containerPort: 50051
          env:
            - name: FLOW_NODE_ID
              value: "sort"
            - name: FLOW_SIDECAR_PORT
              value: "50051"
            - name: OPERATOR_ADDRESS
              value: "flow-operator:50052"
            - name: ARCHIVIST_ADDRESS
              value: "flow-archivist:50054"
            - name: LIBRARIAN_ADDRESS
              value: "flow-librarian:50058"
      volumes:
        - name: node-config
          configMap:
            name: sort-config
---
# --- Appraise ---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: appraise
  namespace: default
  labels:
    app.kubernetes.io/name: appraise
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: appraise
  template:
    metadata:
      labels:
        app.kubernetes.io/name: appraise
        flow.gideas.io/node-name: appraise
    spec:
      containers:
        - name: node
          image: appraise:latest
          ports:
            - containerPort: 50053
          env:
            - name: FLOW_NODE_PORT
              value: "50053"
            - name: OLLAMA_BASE_URL
              value: "http://ollama:11434"
          volumeMounts:
            - name: node-config
              mountPath: /etc/foundry
              readOnly: true
        - name: sidecar
          image: flow-sidecar:latest
          ports:
            - containerPort: 50051
          env:
            - name: FLOW_NODE_ID
              value: "appraise"
            - name: FLOW_SIDECAR_PORT
              value: "50051"
            - name: OPERATOR_ADDRESS
              value: "flow-operator:50052"
            - name: ARCHIVIST_ADDRESS
              value: "flow-archivist:50054"
            - name: LIBRARIAN_ADDRESS
              value: "flow-librarian:50058"
            - name: EVENT_BUS_ADDRESS
              value: "flow-eventbus:50056"
            - name: FRICTION_LEDGER_ADDRESS
              value: "flow-frictionledger:50057"
      volumes:
        - name: node-config
          configMap:
            name: appraise-config
---
# --- Reviewer ---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: reviewer
  namespace: default
  labels:
    app.kubernetes.io/name: reviewer
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: reviewer
  template:
    metadata:
      labels:
        app.kubernetes.io/name: reviewer
        flow.gideas.io/node-name: reviewer
    spec:
      containers:
        - name: node
          image: reviewer:latest
          ports:
            - containerPort: 50053
          env:
            - name: FLOW_NODE_PORT
              value: "50053"
            - name: OLLAMA_BASE_URL
              value: "http://ollama:11434"
          volumeMounts:
            - name: node-config
              mountPath: /etc/foundry
              readOnly: true
        - name: sidecar
          image: flow-sidecar:latest
          ports:
            - containerPort: 50051
          env:
            - name: FLOW_NODE_ID
              value: "reviewer"
            - name: FLOW_SIDECAR_PORT
              value: "50051"
            - name: OPERATOR_ADDRESS
              value: "flow-operator:50052"
            - name: ARCHIVIST_ADDRESS
              value: "flow-archivist:50054"
      volumes:
        - name: node-config
          configMap:
            name: reviewer-config
---
# --- Refine ---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: refine
  namespace: default
  labels:
    app.kubernetes.io/name: refine
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: refine
  template:
    metadata:
      labels:
        app.kubernetes.io/name: refine
        flow.gideas.io/node-name: refine
    spec:
      containers:
        - name: node
          image: refine:latest
          ports:
            - containerPort: 50053
          env:
            - name: FLOW_NODE_PORT
              value: "50053"
            - name: OLLAMA_BASE_URL
              value: "http://ollama:11434"
          volumeMounts:
            - name: node-config
              mountPath: /etc/foundry
              readOnly: true
        - name: sidecar
          image: flow-sidecar:latest
          ports:
            - containerPort: 50051
          env:
            - name: FLOW_NODE_ID
              value: "refine"
            - name: FLOW_SIDECAR_PORT
              value: "50051"
            - name: OPERATOR_ADDRESS
              value: "flow-operator:50052"
            - name: ARCHIVIST_ADDRESS
              value: "flow-archivist:50054"
            - name: LIBRARIAN_ADDRESS
              value: "flow-librarian:50058"
            - name: EVENT_BUS_ADDRESS
              value: "flow-eventbus:50056"
            - name: FRICTION_LEDGER_ADDRESS
              value: "flow-frictionledger:50057"
      volumes:
        - name: node-config
          configMap:
            name: refine-config
---
# --- Facilitator ---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: facilitator
  namespace: default
  labels:
    app.kubernetes.io/name: facilitator
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: facilitator
  template:
    metadata:
      labels:
        app.kubernetes.io/name: facilitator
        flow.gideas.io/node-name: facilitator
    spec:
      containers:
        - name: node
          image: facilitator:latest
          ports:
            - containerPort: 50053
          env:
            - name: FLOW_NODE_PORT
              value: "50053"
          volumeMounts:
            - name: node-config
              mountPath: /etc/foundry
              readOnly: true
        - name: sidecar
          image: flow-sidecar:latest
          ports:
            - containerPort: 50051
          env:
            - name: FLOW_NODE_ID
              value: "facilitator"
            - name: FLOW_SIDECAR_PORT
              value: "50051"
            - name: OPERATOR_ADDRESS
              value: "flow-operator:50052"
            - name: ARCHIVIST_ADDRESS
              value: "flow-archivist:50054"
            - name: LIBRARIAN_ADDRESS
              value: "flow-librarian:50058"
            - name: FRICTION_LEDGER_ADDRESS
              value: "flow-frictionledger:50057"
      volumes:
        - name: node-config
          configMap:
            name: facilitator-config
---
# --- Arbiter ---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: arbiter
  namespace: default
  labels:
    app.kubernetes.io/name: arbiter
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: arbiter
  template:
    metadata:
      labels:
        app.kubernetes.io/name: arbiter
        flow.gideas.io/node-name: arbiter
    spec:
      containers:
        - name: node
          image: arbiter:latest
          ports:
            - containerPort: 50053
          env:
            - name: FLOW_NODE_PORT
              value: "50053"
          volumeMounts:
            - name: node-config
              mountPath: /etc/foundry
              readOnly: true
        - name: sidecar
          image: flow-sidecar:latest
          ports:
            - containerPort: 50051
          env:
            - name: FLOW_NODE_ID
              value: "arbiter"
            - name: FLOW_SIDECAR_PORT
              value: "50051"
            - name: OPERATOR_ADDRESS
              value: "flow-operator:50052"
            - name: ARCHIVIST_ADDRESS
              value: "flow-archivist:50054"
      volumes:
        - name: node-config
          configMap:
            name: arbiter-config
---
# --- Juror ---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: juror
  namespace: default
  labels:
    app.kubernetes.io/name: juror
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: juror
  template:
    metadata:
      labels:
        app.kubernetes.io/name: juror
        flow.gideas.io/node-name: juror
    spec:
      containers:
        - name: node
          image: juror:latest
          ports:
            - containerPort: 50053
          env:
            - name: FLOW_NODE_PORT
              value: "50053"
            - name: OLLAMA_BASE_URL
              value: "http://ollama:11434"
          volumeMounts:
            - name: node-config
              mountPath: /etc/foundry
              readOnly: true
        - name: sidecar
          image: flow-sidecar:latest
          ports:
            - containerPort: 50051
          env:
            - name: FLOW_NODE_ID
              value: "juror"
            - name: FLOW_SIDECAR_PORT
              value: "50051"
            - name: OPERATOR_ADDRESS
              value: "flow-operator:50052"
            - name: ARCHIVIST_ADDRESS
              value: "flow-archivist:50054"
      volumes:
        - name: node-config
          configMap:
            name: juror-config
